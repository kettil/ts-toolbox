interface SwaggerPropertyCommon {
  readonly description?: string;

  // Additional possible parameters
  readonly readOnly?: boolean;
  readonly writeOnly?: boolean;
}

export type { SwaggerPropertyCommon };
